## Kid-safe POS system (Square-like) with menu items, fake payments, gift cards, and simulated online credit cards

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
